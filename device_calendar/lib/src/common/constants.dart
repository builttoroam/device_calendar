class Constants {
  static const String fromJsonMapIsNull = 'The json object is null';

  static const String invalidArgument = 'Provided arguments are invalid';
  static const String createOrUpdateEventArgumentRequirements = 'To create or update an event you must provide calendar id, event with title, start date and end date (where start date must be before end date)';
}
